apiVersion: v1
kind: Service
metadata:
  name: fluentsearch-bff-service
  namespace: fluentsearch
spec:
  type: NodePort
  selector:
    app: fluentsearch-bff
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30009
